---
interface Props {
  productName: string;
  productImage: string;
  price: string;
  originalPrice?: string;
  rating: number;
  affiliateLink: string;
  store?: 'Amazon' | 'Flipkart' | 'Other';
  badge?: string;
}
const { productName, productImage, price, originalPrice, rating, affiliateLink, store = 'Amazon', badge } = Astro.props;

// Generate star rating
const fullStars = Math.floor(rating);
const hasHalf = rating % 1 >= 0.5;
const emptyStars = 5 - fullStars - (hasHalf ? 1 : 0);

const isAmazon = store === 'Amazon';
const isFlipkart = store === 'Flipkart';

let storeColor = 'bg-gray-100 text-gray-800 dark:bg-gray-700 dark:text-gray-200';
if (isAmazon) storeColor = 'bg-orange-100 text-orange-800 dark:bg-orange-900/30 dark:text-orange-300';
if (isFlipkart) storeColor = 'bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300';
---

<div class="card p-6 flex flex-col md:flex-row gap-6 my-6 bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-gray-100 dark:border-gray-700 relative overflow-hidden">
  {badge && (
    <div class="absolute top-0 right-0 rounded-bl-xl rounded-tr-xl bg-gradient-to-r from-primary-500 to-accent-500 text-white px-4 py-1 text-sm font-bold shadow-sm">
      {badge}
    </div>
  )}

  <div class="flex-shrink-0 flex justify-center">
    <img src={productImage} alt={productName} class="rounded-xl object-cover w-48 h-48 border border-gray-100 dark:border-gray-700" />
  </div>

  <div class="flex-col flex flex-grow justify-between">
    <div>
      <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">{productName}</h3>
      
      <div class="flex items-center gap-1 mb-3">
        <span class="text-yellow-400 text-lg flex" aria-label={`Rating: ${rating} out of 5`}>
          {Array(fullStars).fill('★').map((star) => <span class="text-yellow-400">{star}</span>)}
          {hasHalf && <span class="text-yellow-400">★</span> /* Ideally a half-star icon */}
          {Array(emptyStars).fill('☆').map((star) => <span class="text-gray-300 dark:text-gray-600">{star}</span>)}
        </span>
        <span class="text-sm font-medium text-gray-600 dark:text-gray-400 ml-2">{rating.toFixed(1)}</span>
      </div>

      <div class="flex items-end gap-3 mb-4">
        <span class="text-2xl font-bold text-green-600 dark:text-green-500">{price}</span>
        {originalPrice && (
          <span class="line-through text-gray-400 text-sm mb-1">{originalPrice}</span>
        )}
        <span class={`text-xs font-semibold px-2 py-0.5 rounded-md mb-1 ${storeColor}`}>
          {store}
        </span>
      </div>
    </div>

    <div class="flex flex-col gap-3 mt-4">
      <a 
        href={affiliateLink} 
        rel="nofollow noopener sponsored" 
        target="_blank" 
        class="btn-primary w-full md:w-auto inline-flex justify-center items-center px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white rounded-xl font-semibold transition-colors duration-200"
      >
        Buy Now on {store} &rarr;
      </a>

    </div>
  </div>
</div>
